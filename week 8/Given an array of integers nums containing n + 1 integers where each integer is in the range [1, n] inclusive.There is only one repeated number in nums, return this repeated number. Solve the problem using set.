def find_duplicate(nums):
    seen = set()
    for num in nums:
        if num in seen:
            return num
        seen.add(num)
    return -1


nums1 = input().split()
nums1=[int(i) for i in nums1]
print(find_duplicate(nums1))  
