# fibonacci
def fiba(n):
    if n == 1:
        return 0
    if n == 2:
        return 1
    return fiba(n - 1) + fiba(n - 2)

n = int(input())
d = 0

for i in range(1, n + 2):
         c = fiba(i)
          d = d + (c * c)
    print(d)
