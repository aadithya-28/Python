n=int(input())
d=dict()

for i in range(0,n):
    a=input()
    if a not in d:
        d[a]=1
    else:
        d[a]=d[a]+1
l=list(d.values())
a=max(l)
l1=[]
for i in d:
    if d[i]==a:
        l1.append(i)
print(min(l1))
        
